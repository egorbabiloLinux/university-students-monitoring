<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8" />
    <title>Дашборд студентов</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"> <!-- убрать бутстрапы!-->
    <link rel="stylesheet" href="/css/style.css">

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <title>UniversityStudentsMonitoring - <%= title %></title>
</head>
<body class="p-4">
    <div class="container">
        <h2 class="mb-4">Дашборд студентов по индексу риска</h2>


        <form method="GET" class="row g-3 mb-4" id="filterForm">
            <div class="col-md-4">
                <label class="form-label">Факультет</label>
                <select class="filter-select" name="faculty">
                    <option value="">Все</option>
                    <% faculties.forEach(f => { %>
                        <option value="<%= f %>" <%= selected.faculty === f ? 'selected' : '' %>><%= f %></option>
                    <% }) %>
                </select>
            </div>

            <div class="col-md-3">
                <label class="form-label">Группа</label>
                <select class="filter-select" name="group">
                    <option value="">Все</option>
                    <% groups.forEach(g => { %>
                        <option value="<%= g %>" <%= selected.group == g ? 'selected' : '' %>><%= g %></option>
                    <% }) %>
                </select>
            </div>

            <div class="col-md-2 d-flex align-items-end">
                <button class="button" style="width: 200px; height: 38px;" type="submit">Фильтр</button>
            </div>
        </form>

        <table class="table table-hover align-middle" id="studentsTable">
            <thead>
                <tr>
                    <th>ФИО</th>
                    <th>Факультет</th>
                    <th>Группа</th>
                    <th>Средний балл</th>
                    <th>Индекс риска</th>
                </tr>
            </thead>
            <tbody></tbody>
        </table>
    </div>

    <script src="/js/riskDashboard.js"></script>
</body>
</html>